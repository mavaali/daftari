# HIDDEN solution key — GM only. NEVER place inside the player-visible case vault.
# Tutorial case: NO frame/trap. The GM coaches the loop rather than springing a trap.
solution:
  case: dead-drop
  verdict_axis: location
  ground_truth: { location: site-7 }
  frame: null                       # tutorial: no disinformation, no framed answer
  sacred_tensions_must_stay_open: []  # the dead-drop-site tension is RESOLVABLE, not sacred

  # Key tells the GM watches for (map to the loop the tutorial teaches)
  key_tells:
    weak_source:   fr-t02   # WREN D4 — honest but mistaken; single glimpse, poor light. Do NOT credit it.
    corroborator:  fr-t03   # independent A2 intercept — the second source that promotes working->evergreen
    primary:       fr-t01   # SPARROW B2 — Side A, corroborated by fr-t03

  # Verdict rubric (a tutorial is graded gently — the point is the loop, not a trap)
  burned_if:                # only a genuine loop-failure "fails" the tutorial
    - "names site-4 by trusting WREN's D4 over SPARROW's B2 without corroborating"
    - "closes/resolves the tension by picking the higher grade instead of finding an independent source"
  clean_win_requires:       # STATION CHIEF
    - "names site-7"
    - "corroborated site-7 via TWO independent sources (fr-t01 AND fr-t03) before naming"
    - "held the site-7-vs-site-4 disagreement as a tension rather than picking a side on grade alone"
  analyst_if:               # correct answer, loop half-done
    - "named site-7 on SPARROW alone (B2) without seeking the independent A2 corroborator"
    - "never logged the contradiction as a tension"
